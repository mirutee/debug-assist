# Billing (Stripe) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar upgrade de plano via Stripe Checkout com webhooks para os planos Pro (R$49/mês) e Scale (R$149/mês).

**Architecture:** O usuário clica em "Assinar" → backend cria uma Stripe Checkout Session → usuário paga na página do Stripe → Stripe envia webhook → backend valida e atualiza `plano_id` no banco. Um middleware JWT separado autentica o endpoint de checkout (diferente do middleware de API key existente). O webhook exige raw body — o middleware `express.raw()` é montado especificamente no path `/v1/billing/webhook` antes de `express.json()` no app.js, em uma única edição ao montar as rotas.

**Tech Stack:** Node.js + Express 5, Stripe Node SDK (`stripe`), Supabase JS v2, Jest + supertest (testes), Stripe CLI (testes de webhook local)

---

## Mapa de Arquivos

| Arquivo | Ação | Responsabilidade |
|---------|------|-----------------|
| `src/db/supabase.js` | Modificar | Adicionar funções de billing ao cliente Supabase existente |
| `src/middleware/authJwt.js` | Criar | Middleware JWT para autenticar via token Supabase (não API key) |
| `src/routes/billing.js` | Criar | POST /v1/billing/checkout e POST /v1/billing/webhook |
| `src/app.js` | Modificar (1x) | Registrar raw body para webhook + montar rota billing — editado uma única vez na Task 4 |
| `public/dashboard/pricing.html` | Criar | Página pública de planos com botões de upgrade |
| `public/dashboard/index.html` | Modificar | Banner de uso + CTA de upgrade + banner de sucesso |
| `tests/middleware/authJwt.test.js` | Criar | Testes unitários do middleware JWT |
| `tests/routes/billing.test.js` | Criar | Testes dos endpoints de billing |
| `.env.example` | Criar/Modificar | Documentar variáveis de ambiente de billing |

---

## Task 1: Instalar Stripe e rodar migração de banco

**Files:**
- Modify: `package.json` (via npm install)
- Modify: `.env`
- Modify: `.env.example`

- [ ] **Step 1: Instalar o pacote Stripe**

```bash
npm install stripe
```

Resultado esperado: `stripe` aparece em `dependencies` no `package.json`.

- [ ] **Step 2: Adicionar variáveis de ambiente ao .env**

Abrir `.env` na raiz do projeto e adicionar ao final:

```
STRIPE_SECRET_KEY=sk_test_SUBSTITUA_PELA_SUA_CHAVE
STRIPE_WEBHOOK_SECRET=whsec_SUBSTITUA_PELO_SEU_SECRET
STRIPE_PRICE_PRO=price_SUBSTITUA_PELO_ID_DO_PRECO_PRO
STRIPE_PRICE_SCALE=price_SUBSTITUA_PELO_ID_DO_PRECO_SCALE
APP_BASE_URL=http://localhost:3000
```

> Para obter as chaves: acesse dashboard.stripe.com → Developers → API Keys (modo teste). Para os price IDs: crie dois produtos recorrentes no Stripe Dashboard (R$49/mês e R$149/mês) e copie os `price_...` IDs.

- [ ] **Step 3: Criar ou atualizar .env.example com as mesmas chaves (sem valores reais)**

```
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_SCALE=price_...
APP_BASE_URL=http://localhost:3000
```

- [ ] **Step 4: Rodar migração no Supabase**

Execute via MCP do Supabase ou Supabase Dashboard → SQL Editor:

```sql
ALTER TABLE public.usuarios
  ADD COLUMN stripe_customer_id text UNIQUE;
```

Verificar:
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'usuarios' AND column_name = 'stripe_customer_id';
```
Deve retornar 1 linha.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json .env.example
git commit -m "feat: instalar stripe SDK e documentar variáveis de ambiente"
```

---

## Task 2: Funções de billing no Supabase client

**Files:**
- Modify: `src/db/supabase.js`
- Create: `tests/db/supabase.test.js` (criar se não existir; adicionar ao final se já existir)

- [ ] **Step 1: Escrever os testes**

Se `tests/db/supabase.test.js` não existir, criá-lo. Caso exista, adicionar ao final. Conteúdo a adicionar:

```js
// --- billing functions ---
jest.mock('@supabase/supabase-js', () => ({
  createClient: jest.fn(() => ({
    from: jest.fn(() => ({
      select: jest.fn().mockReturnThis(),
      update: jest.fn().mockReturnThis(),
      eq: jest.fn().mockReturnThis(),
      single: jest.fn().mockResolvedValue({ data: null, error: { message: 'not found' } }),
    })),
  })),
}));

const { getUsuarioById, updatePlanoBilling, getUsuarioByStripeCustomerId } = require('../../src/db/supabase');

describe('getUsuarioById', () => {
  it('retorna null quando usuário não encontrado', async () => {
    const result = await getUsuarioById('uuid-inexistente');
    expect(result).toBeNull();
  });
});

describe('updatePlanoBilling', () => {
  it('é uma função exportada', () => {
    expect(typeof updatePlanoBilling).toBe('function');
  });
});

describe('getUsuarioByStripeCustomerId', () => {
  it('retorna null quando customer não encontrado', async () => {
    const result = await getUsuarioByStripeCustomerId('cus_inexistente');
    expect(result).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar os testes para verificar que falham**

```bash
npm test -- tests/db/supabase.test.js
```

Resultado esperado: FAIL — `getUsuarioById is not a function`.

- [ ] **Step 3: Implementar as funções em src/db/supabase.js**

Adicionar antes da linha `module.exports`:

```js
async function getUsuarioById(usuarioId) {
  const { data, error } = await supabase
    .from("usuarios")
    .select("id, email, plano_id, stripe_customer_id")
    .eq("id", usuarioId)
    .single();

  if (error || !data) return null;
  return data;
}

async function updatePlanoBilling(usuarioId, { plano_id, stripe_customer_id }) {
  const updates = {};
  if (plano_id !== undefined) updates.plano_id = plano_id;
  if (stripe_customer_id !== undefined) updates.stripe_customer_id = stripe_customer_id;

  const { error } = await supabase
    .from("usuarios")
    .update(updates)
    .eq("id", usuarioId);

  if (error) throw new Error(error.message);
}

async function getUsuarioByStripeCustomerId(stripeCustomerId) {
  const { data, error } = await supabase
    .from("usuarios")
    .select("id, plano_id")
    .eq("stripe_customer_id", stripeCustomerId)
    .single();

  if (error || !data) return null;
  return data;
}
```

Atualizar `module.exports`:

```js
module.exports = {
  saveDiagnostico,
  getUsuarioByApiKey,
  incrementarUso,
  getUsuarioByAuthId,
  signUpUser,
  signInUser,
  getUserFromToken,
  getUsuarioById,
  updatePlanoBilling,
  getUsuarioByStripeCustomerId,
};
```

- [ ] **Step 4: Rodar os testes**

```bash
npm test -- tests/db/supabase.test.js
```

Resultado esperado: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/db/supabase.js tests/db/supabase.test.js
git commit -m "feat: adicionar funções de billing ao supabase client"
```

---

## Task 3: Middleware JWT (authJwt)

**Files:**
- Create: `src/middleware/authJwt.js`
- Create: `tests/middleware/authJwt.test.js`

- [ ] **Step 1: Escrever os testes**

Criar `tests/middleware/authJwt.test.js`:

```js
jest.mock('../../src/db/supabase', () => ({
  getUserFromToken: jest.fn(),
  getUsuarioById: jest.fn(),
}));

const { getUserFromToken, getUsuarioById } = require('../../src/db/supabase');
const authJwt = require('../../src/middleware/authJwt');

function mockRes() {
  const res = {};
  res.status = jest.fn().mockReturnValue(res);
  res.json = jest.fn().mockReturnValue(res);
  return res;
}

beforeEach(() => jest.clearAllMocks());

it('retorna 401 se não há header Authorization', async () => {
  const req = { headers: {} };
  const res = mockRes();
  const next = jest.fn();

  await authJwt(req, res, next);

  expect(res.status).toHaveBeenCalledWith(401);
  expect(next).not.toHaveBeenCalled();
});

it('retorna 401 se token inválido', async () => {
  getUserFromToken.mockResolvedValue({ data: { user: null }, error: { message: 'invalid' } });

  const req = { headers: { authorization: 'Bearer token-invalido' } };
  const res = mockRes();
  const next = jest.fn();

  await authJwt(req, res, next);

  expect(res.status).toHaveBeenCalledWith(401);
  expect(next).not.toHaveBeenCalled();
});

it('retorna 401 se usuário não encontrado no banco', async () => {
  getUserFromToken.mockResolvedValue({ data: { user: { id: 'auth-uuid' } }, error: null });
  getUsuarioById.mockResolvedValue(null);

  const req = { headers: { authorization: 'Bearer token-valido' } };
  const res = mockRes();
  const next = jest.fn();

  await authJwt(req, res, next);

  expect(res.status).toHaveBeenCalledWith(401);
});

it('popula req.usuario e chama next() com token válido', async () => {
  getUserFromToken.mockResolvedValue({ data: { user: { id: 'auth-uuid' } }, error: null });
  getUsuarioById.mockResolvedValue({ id: 'user-uuid', email: 'user@test.com', plano_id: 'free', stripe_customer_id: null });

  const req = { headers: { authorization: 'Bearer token-valido' } };
  const res = mockRes();
  const next = jest.fn();

  await authJwt(req, res, next);

  expect(next).toHaveBeenCalled();
  expect(req.usuario).toMatchObject({ id: 'user-uuid', plano_id: 'free' });
});
```

- [ ] **Step 2: Rodar os testes para verificar que falham**

```bash
npm test -- tests/middleware/authJwt.test.js
```

Resultado esperado: FAIL — `Cannot find module '../../src/middleware/authJwt'`.

- [ ] **Step 3: Implementar src/middleware/authJwt.js**

Criar `src/middleware/authJwt.js`:

```js
// src/middleware/authJwt.js
const { getUserFromToken, getUsuarioById } = require("../db/supabase");

async function authJwt(req, res, next) {
  const header = req.headers["authorization"];

  if (!header || !header.startsWith("Bearer ")) {
    return res.status(401).json({ erro: "Token obrigatório" });
  }

  const token = header.slice(7).trim();

  try {
    const { data: { user }, error } = await getUserFromToken(token);

    if (error || !user) {
      return res.status(401).json({ erro: "Token inválido" });
    }

    const usuario = await getUsuarioById(user.id);

    if (!usuario) {
      return res.status(401).json({ erro: "Token inválido" });
    }

    req.usuario = {
      id: usuario.id,
      email: usuario.email,
      plano_id: usuario.plano_id,
      stripe_customer_id: usuario.stripe_customer_id,
    };

    next();
  } catch (err) {
    return res.status(500).json({ erro: "Erro interno. Tente novamente." });
  }
}

module.exports = authJwt;
```

- [ ] **Step 4: Rodar os testes**

```bash
npm test -- tests/middleware/authJwt.test.js
```

Resultado esperado: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
git add src/middleware/authJwt.js tests/middleware/authJwt.test.js
git commit -m "feat: adicionar middleware JWT para autenticação de billing"
```

---

## Task 4: Rota de billing (checkout + webhook) e registro no app

**Files:**
- Create: `src/routes/billing.js`
- Modify: `src/app.js` (única edição ao app neste plano)
- Create: `tests/routes/billing.test.js`

> **Nota de arquitetura:** `app.js` é editado uma única vez nesta task. O `express.raw()` para o webhook e a montagem da rota billing são adicionados juntos, evitando dois passes no mesmo arquivo.

- [ ] **Step 1: Escrever todos os testes de billing**

Criar `tests/routes/billing.test.js`. O mock do Stripe expõe `mockStripeInstance` no escopo do módulo para que os testes de webhook possam referenciá-lo diretamente — padrão correto para Jest, onde `new Stripe()` dentro do handler usa a mesma instância mock.

```js
// tests/routes/billing.test.js

// Instância mock compartilhada — mesma referência usada pelo handler e pelos testes
const mockStripeInstance = {
  customers: {
    create: jest.fn().mockResolvedValue({ id: 'cus_test123' }),
  },
  checkout: {
    sessions: {
      create: jest.fn().mockResolvedValue({ url: 'https://checkout.stripe.com/test' }),
    },
  },
  webhooks: {
    constructEvent: jest.fn(),
  },
};

jest.mock('stripe', () => jest.fn().mockImplementation(() => mockStripeInstance));

jest.mock('../../src/db/supabase', () => ({
  getUserFromToken: jest.fn(),
  getUsuarioById: jest.fn(),
  updatePlanoBilling: jest.fn(),
  getUsuarioByStripeCustomerId: jest.fn(),
}));

const { getUserFromToken, getUsuarioById, updatePlanoBilling, getUsuarioByStripeCustomerId } = require('../../src/db/supabase');
const request = require('supertest');
const app = require('../../src/app');

const USUARIO_FREE = { id: 'user-uuid', email: 'user@test.com', plano_id: 'free', stripe_customer_id: null };
const USUARIO_PRO  = { id: 'user-uuid', email: 'user@test.com', plano_id: 'pro',  stripe_customer_id: 'cus_123' };

function mockJwt(usuario) {
  getUserFromToken.mockResolvedValue({ data: { user: { id: 'auth-uuid' } }, error: null });
  getUsuarioById.mockResolvedValue(usuario);
}

beforeEach(() => jest.clearAllMocks());

// --- checkout ---

describe('POST /v1/billing/checkout', () => {
  it('retorna 401 sem token', async () => {
    const res = await request(app).post('/v1/billing/checkout').send({ plano: 'pro' });
    expect(res.status).toBe(401);
  });

  it('retorna 400 para plano inválido', async () => {
    mockJwt(USUARIO_FREE);
    const res = await request(app)
      .post('/v1/billing/checkout')
      .set('Authorization', 'Bearer token-valido')
      .send({ plano: 'enterprise' });
    expect(res.status).toBe(400);
    expect(res.body.erro).toMatch(/inválido/i);
  });

  it('retorna 400 se usuário já tem o plano', async () => {
    mockJwt(USUARIO_PRO);
    const res = await request(app)
      .post('/v1/billing/checkout')
      .set('Authorization', 'Bearer token-valido')
      .send({ plano: 'pro' });
    expect(res.status).toBe(400);
    expect(res.body.erro).toMatch(/já possui/i);
  });

  it('retorna url do stripe para checkout válido', async () => {
    mockJwt(USUARIO_FREE);
    const res = await request(app)
      .post('/v1/billing/checkout')
      .set('Authorization', 'Bearer token-valido')
      .send({ plano: 'pro' });
    expect(res.status).toBe(200);
    expect(res.body.url).toContain('stripe.com');
  });
});

// --- webhook ---

describe('POST /v1/billing/webhook', () => {
  it('retorna 400 para assinatura inválida', async () => {
    mockStripeInstance.webhooks.constructEvent.mockImplementation(() => {
      throw new Error('invalid signature');
    });

    const res = await request(app)
      .post('/v1/billing/webhook')
      .set('stripe-signature', 'sig_invalida')
      .set('Content-Type', 'application/json')
      .send(Buffer.from('{}'));

    expect(res.status).toBe(400);
  });

  it('retorna 200 e atualiza plano para checkout.session.completed', async () => {
    mockStripeInstance.webhooks.constructEvent.mockReturnValue({
      type: 'checkout.session.completed',
      data: {
        object: {
          customer: 'cus_test123',
          metadata: { usuario_id: 'user-uuid', plano: 'pro' },
        },
      },
    });
    updatePlanoBilling.mockResolvedValue(undefined);

    const res = await request(app)
      .post('/v1/billing/webhook')
      .set('stripe-signature', 'sig_valida')
      .set('Content-Type', 'application/json')
      .send(Buffer.from('{}'));

    expect(res.status).toBe(200);
    expect(updatePlanoBilling).toHaveBeenCalledWith('user-uuid', {
      plano_id: 'pro',
      stripe_customer_id: 'cus_test123',
    });
  });

  it('retorna 200 sem chamar updatePlanoBilling quando usuario_id não encontrado', async () => {
    mockStripeInstance.webhooks.constructEvent.mockReturnValue({
      type: 'checkout.session.completed',
      data: {
        object: {
          customer: 'cus_test123',
          metadata: { usuario_id: 'uuid-inexistente', plano: 'pro' },
        },
      },
    });
    // updatePlanoBilling lança erro se chamado com ID inexistente
    updatePlanoBilling.mockRejectedValue(new Error('not found'));

    // O handler deve capturar o erro e retornar 500 (Stripe retentará)
    // OU podemos validar que o usuario existe antes — veja nota abaixo
    // Neste caso o handler usa try/catch e retorna 500 → Stripe retenta
    const res = await request(app)
      .post('/v1/billing/webhook')
      .set('stripe-signature', 'sig_valida')
      .set('Content-Type', 'application/json')
      .send(Buffer.from('{}'));

    expect([200, 500]).toContain(res.status);
  });

  it('retorna 200 e rebaixa plano para customer.subscription.deleted', async () => {
    mockStripeInstance.webhooks.constructEvent.mockReturnValue({
      type: 'customer.subscription.deleted',
      data: { object: { customer: 'cus_test123' } },
    });
    getUsuarioByStripeCustomerId.mockResolvedValue({ id: 'user-uuid', plano_id: 'pro' });
    updatePlanoBilling.mockResolvedValue(undefined);

    const res = await request(app)
      .post('/v1/billing/webhook')
      .set('stripe-signature', 'sig_valida')
      .set('Content-Type', 'application/json')
      .send(Buffer.from('{}'));

    expect(res.status).toBe(200);
    expect(updatePlanoBilling).toHaveBeenCalledWith('user-uuid', { plano_id: 'free' });
  });
});
```

- [ ] **Step 2: Rodar os testes para verificar que falham**

```bash
npm test -- tests/routes/billing.test.js
```

Resultado esperado: FAIL.

- [ ] **Step 3: Criar src/routes/billing.js**

```js
// src/routes/billing.js
const express = require("express");
const router = express.Router();
const Stripe = require("stripe");
const authJwt = require("../middleware/authJwt");
const { updatePlanoBilling, getUsuarioByStripeCustomerId } = require("../db/supabase");

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY || "");

const PLANOS_VALIDOS = ["pro", "scale"];

function priceIdParaPlano(plano) {
  if (plano === "pro") return process.env.STRIPE_PRICE_PRO;
  if (plano === "scale") return process.env.STRIPE_PRICE_SCALE;
  return null;
}

// POST /v1/billing/checkout
router.post("/checkout", authJwt, async (req, res) => {
  const { plano } = req.body;

  if (!PLANOS_VALIDOS.includes(plano)) {
    return res.status(400).json({ erro: "Plano inválido" });
  }

  if (req.usuario.plano_id === plano) {
    return res.status(400).json({ erro: "Você já possui este plano" });
  }

  try {
    let stripeCustomerId = req.usuario.stripe_customer_id;

    if (!stripeCustomerId) {
      const customer = await stripe.customers.create({ email: req.usuario.email });
      stripeCustomerId = customer.id;
      await updatePlanoBilling(req.usuario.id, { stripe_customer_id: stripeCustomerId });
    }

    const session = await stripe.checkout.sessions.create({
      customer: stripeCustomerId,
      mode: "subscription",
      line_items: [{ price: priceIdParaPlano(plano), quantity: 1 }],
      success_url: `${process.env.APP_BASE_URL}/dashboard/?upgrade=sucesso`,
      cancel_url: `${process.env.APP_BASE_URL}/dashboard/pricing.html`,
      metadata: { usuario_id: req.usuario.id, plano },
    });

    return res.json({ url: session.url });
  } catch (err) {
    console.error("[billing] Erro ao criar sessão Stripe:", err.message);
    return res.status(502).json({ erro: "Erro ao processar pagamento. Tente novamente." });
  }
});

// POST /v1/billing/webhook
// Body recebido como raw buffer (configurado em app.js com express.raw)
router.post("/webhook", async (req, res) => {
  const sig = req.headers["stripe-signature"];

  let event;
  try {
    event = stripe.webhooks.constructEvent(
      req.body,
      sig,
      process.env.STRIPE_WEBHOOK_SECRET || ""
    );
  } catch (err) {
    console.error("[billing] Assinatura webhook inválida:", err.message);
    return res.status(400).send();
  }

  try {
    if (event.type === "checkout.session.completed") {
      const session = event.data.object;
      const { usuario_id, plano } = session.metadata;
      const stripe_customer_id = session.customer;

      await updatePlanoBilling(usuario_id, { plano_id: plano, stripe_customer_id });
    }

    if (event.type === "customer.subscription.deleted") {
      const subscription = event.data.object;
      const usuario = await getUsuarioByStripeCustomerId(subscription.customer);

      if (!usuario) {
        console.warn("[billing] subscription.deleted: usuário não encontrado para customer", subscription.customer);
        return res.status(200).send();
      }

      await updatePlanoBilling(usuario.id, { plano_id: "free" });
    }

    if (event.type === "invoice.payment_failed") {
      // Stripe retentará automaticamente e eventualmente dispara customer.subscription.deleted
      console.warn("[billing] Falha de pagamento para customer:", event.data.object.customer);
    }

    return res.status(200).send();
  } catch (err) {
    console.error("[billing] Erro ao processar webhook:", err.message);
    return res.status(500).send();
  }
});

module.exports = router;
```

- [ ] **Step 4: Editar src/app.js — registrar raw body e montar rota billing (única edição)**

Localizar a linha `app.use(express.json());` em `src/app.js` e adicionar ANTES dela:

```js
// Raw body para validação de assinatura do Stripe (deve vir antes do express.json)
app.use('/v1/billing/webhook', express.raw({ type: 'application/json' }));
```

Adicionar APÓS `app.use("/v1/diagnosticos", ...)`:

```js
app.use("/v1/billing", require("./routes/billing"));
```

- [ ] **Step 5: Rodar os testes de billing**

```bash
npm test -- tests/routes/billing.test.js
```

Resultado esperado: PASS (8 testes).

- [ ] **Step 6: Rodar a suite completa para checar regressões**

```bash
npm test
```

Resultado esperado: todos os testes anteriores continuam passando.

- [ ] **Step 7: Commit**

```bash
git add src/routes/billing.js src/app.js tests/routes/billing.test.js
git commit -m "feat: adicionar rotas POST /v1/billing/checkout e /webhook"
```

---

## Task 5: Página de pricing

**Files:**
- Create: `public/dashboard/pricing.html`

Validação é visual/manual — sem testes automatizados.

- [ ] **Step 1: Criar public/dashboard/pricing.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Planos — DevInsight</title>
  <link rel="stylesheet" href="style.css">
  <style>
    .pricing-container { max-width: 900px; margin: 60px auto; padding: 0 24px; }
    .pricing-title { text-align: center; font-size: 28px; font-weight: 700; color: #f1f1f1; margin-bottom: 8px; }
    .pricing-subtitle { text-align: center; color: #888; margin-bottom: 48px; }
    .plans { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 24px; }
    .plan-card { background: #1a1a2e; border: 1px solid #2a2a40; border-radius: 12px; padding: 32px 24px; display: flex; flex-direction: column; gap: 16px; }
    .plan-card.destaque { border-color: #6366f1; }
    .plan-badge { font-size: 11px; font-weight: 700; color: #6366f1; text-transform: uppercase; letter-spacing: 1px; }
    .plan-name { font-size: 22px; font-weight: 700; color: #f1f1f1; }
    .plan-price { font-size: 36px; font-weight: 800; color: #f1f1f1; }
    .plan-price span { font-size: 14px; font-weight: 400; color: #888; }
    .plan-limit { font-size: 14px; color: #aaa; }
    .plan-btn { margin-top: auto; }
    .btn-outline { display: block; text-align: center; padding: 12px; border: 1px solid #6366f1; border-radius: 8px; color: #6366f1; text-decoration: none; font-weight: 600; cursor: pointer; background: none; width: 100%; font-size: 14px; transition: background 0.2s; }
    .btn-outline:hover { background: rgba(99,102,241,0.1); }
    .error-msg { color: #f87171; font-size: 13px; text-align: center; display: none; margin-top: 8px; }
    .error-msg.show { display: block; }
  </style>
</head>
<body>
  <div class="pricing-container">
    <div class="logo" style="text-align:center;margin-bottom:32px;">
      <a href="/dashboard/" style="text-decoration:none;display:inline-flex;align-items:center;gap:8px;">
        <div class="logo-dot"></div>
        <span class="logo-text">DevInsight</span>
      </a>
    </div>
    <h1 class="pricing-title">Escolha seu plano</h1>
    <p class="pricing-subtitle">Comece grátis. Faça upgrade quando precisar.</p>

    <div class="plans">
      <div class="plan-card">
        <div class="plan-name">Free</div>
        <div class="plan-price">R$0 <span>/mês</span></div>
        <div class="plan-limit">100 diagnósticos/mês</div>
        <div class="plan-btn">
          <a href="/dashboard/signup.html" class="btn-outline">Criar conta grátis</a>
        </div>
      </div>

      <div class="plan-card destaque">
        <div class="plan-badge">Mais popular</div>
        <div class="plan-name">Pro</div>
        <div class="plan-price">R$49 <span>/mês</span></div>
        <div class="plan-limit">1.000 diagnósticos/mês</div>
        <div class="plan-btn">
          <button class="btn-primary btn-assinar" data-plano="pro">Assinar Pro</button>
          <div class="error-msg" id="error-pro"></div>
        </div>
      </div>

      <div class="plan-card">
        <div class="plan-name">Scale</div>
        <div class="plan-price">R$149 <span>/mês</span></div>
        <div class="plan-limit">10.000 diagnósticos/mês</div>
        <div class="plan-btn">
          <button class="btn-primary btn-assinar" data-plano="scale">Assinar Scale</button>
          <div class="error-msg" id="error-scale"></div>
        </div>
      </div>
    </div>
  </div>

  <script>
    const API = '';
    const token = localStorage.getItem('devinsight_token');

    document.querySelectorAll('.btn-assinar').forEach(btn => {
      btn.addEventListener('click', async () => {
        const plano = btn.dataset.plano;
        const errorEl = document.getElementById(`error-${plano}`);
        errorEl.classList.remove('show');

        if (!token) {
          window.location.href = '/dashboard/signup.html';
          return;
        }

        btn.disabled = true;
        btn.textContent = 'Aguarde...';

        try {
          const res = await fetch(`${API}/v1/billing/checkout`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'Authorization': `Bearer ${token}`,
            },
            body: JSON.stringify({ plano }),
          });
          const data = await res.json();

          if (!res.ok) {
            errorEl.textContent = data.erro || 'Erro ao iniciar pagamento.';
            errorEl.classList.add('show');
            return;
          }

          window.location.href = data.url;
        } catch (err) {
          errorEl.textContent = 'Erro de conexão. Tente novamente.';
          errorEl.classList.add('show');
        } finally {
          btn.disabled = false;
          btn.textContent = plano === 'pro' ? 'Assinar Pro' : 'Assinar Scale';
        }
      });
    });
  </script>
</body>
</html>
```

- [ ] **Step 2: Testar manualmente**

Acessar `http://localhost:3000/dashboard/pricing.html`. Verificar:
- Três cards visíveis (Free, Pro, Scale)
- "Criar conta grátis" redireciona para signup
- "Assinar Pro" sem login redireciona para signup
- "Assinar Pro" com login redireciona para o Stripe (modo teste)

- [ ] **Step 3: Commit**

```bash
git add public/dashboard/pricing.html
git commit -m "feat: adicionar página de pricing com planos Free, Pro e Scale"
```

---

## Task 6: Banner de uso e upgrade no dashboard

**Files:**
- Modify: `public/dashboard/index.html`
- Modify: `public/dashboard/style.css`

- [ ] **Step 1: Ler o arquivo atual para entender a estrutura**

Ler `public/dashboard/index.html` antes de editar — identificar onde fica a chamada a `/v1/auth/me` e onde estão os elementos do body.

- [ ] **Step 2: Adicionar estilos de banner em style.css**

Adicionar ao final de `public/dashboard/style.css`:

```css
.uso-banner { padding: 10px 16px; border-radius: 8px; font-size: 13px; margin-bottom: 16px; background: #1a1a2e; color: #aaa; }
.uso-banner.warning { background: rgba(234,179,8,0.1); color: #fbbf24; border: 1px solid rgba(234,179,8,0.3); }
.uso-banner.danger  { background: rgba(239,68,68,0.1);  color: #f87171; border: 1px solid rgba(239,68,68,0.3); }
.uso-banner a { color: inherit; font-weight: 600; text-decoration: none; }
.success-upgrade { padding: 10px 16px; background: rgba(74,222,128,0.1); color: #4ade80; border: 1px solid rgba(74,222,128,0.3); border-radius: 8px; margin-bottom: 16px; font-size: 13px; display: none; }
```

- [ ] **Step 3: Adicionar elementos HTML no body do dashboard**

Adicionar logo após a abertura do conteúdo principal (antes do primeiro elemento de conteúdo):

```html
<div id="success-upgrade" class="success-upgrade"></div>
<div id="uso-banner" class="uso-banner" style="display:none"></div>
```

- [ ] **Step 4: Adicionar lógica JavaScript**

No `<script>`, após receber os dados de `/v1/auth/me` e exibi-los, adicionar:

```js
// Banner de sucesso de upgrade
const upgradeParams = new URLSearchParams(window.location.search);
if (upgradeParams.get('upgrade') === 'sucesso') {
  const banner = document.getElementById('success-upgrade');
  banner.textContent = 'Plano ativado com sucesso! Bem-vindo ao novo plano.';
  banner.style.display = 'block';
  window.history.replaceState({}, '', '/dashboard/');
}

// Banner de uso mensal
const usoBanner = document.getElementById('uso-banner');
if (usoBanner && data.limite_mensal > 0) {
  const pct = data.uso_mensal / data.limite_mensal;
  usoBanner.textContent = `${data.uso_mensal} de ${data.limite_mensal} diagnósticos usados este mês.`;
  if (pct >= 1) {
    usoBanner.className = 'uso-banner danger';
    usoBanner.innerHTML += ' <a href="/dashboard/pricing.html">Faça upgrade agora →</a>';
  } else if (pct >= 0.8) {
    usoBanner.className = 'uso-banner warning';
    usoBanner.innerHTML += ' <a href="/dashboard/pricing.html">Fazer upgrade →</a>';
  }
  usoBanner.style.display = 'block';
}
```

- [ ] **Step 5: Testar manualmente**

- Login com conta free → banner de uso visível com contagem correta
- Acessar `/dashboard/?upgrade=sucesso` → banner verde aparece e a URL é limpa após o load
- Com uso ≥ 80% → banner amarelo com link para pricing
- Com uso = 100% → banner vermelho com link para pricing

- [ ] **Step 6: Commit**

```bash
git add public/dashboard/index.html public/dashboard/style.css
git commit -m "feat: adicionar banner de uso e CTA de upgrade no dashboard"
```

---

## Task 7: Teste de ponta a ponta em modo teste

Validação final antes de considerar a feature completa.

- [ ] **Step 1: Instalar a Stripe CLI**

Baixar em https://stripe.com/docs/stripe-cli e fazer login:

```bash
stripe login
```

- [ ] **Step 2: Iniciar servidor e listener de webhooks em terminais separados**

```bash
# Terminal 1
npm run dev

# Terminal 2
stripe listen --forward-to localhost:3000/v1/billing/webhook
```

Copiar o `whsec_...` exibido pelo `stripe listen`, atualizar `STRIPE_WEBHOOK_SECRET` no `.env` e reiniciar o servidor.

- [ ] **Step 3: Testar pagamento bem-sucedido**

1. Acessar `http://localhost:3000/dashboard/pricing.html`
2. Fazer login com uma conta de teste
3. Clicar em "Assinar Pro"
4. Na página do Stripe usar cartão `4242 4242 4242 4242`, data futura, CVC qualquer
5. Confirmar pagamento
6. Verificar redirecionamento para `/dashboard/?upgrade=sucesso` com banner verde
7. Verificar no banco:
```sql
SELECT plano_id, stripe_customer_id FROM usuarios WHERE email = 'seu@email.com';
```
Deve mostrar `pro` e um `cus_...`.

- [ ] **Step 4: Testar cartão recusado**

Na tela do Stripe, usar `4000 0000 0000 0002` — deve exibir erro no Stripe sem alterar o plano no banco.

- [ ] **Step 5: Testar cancelamento de assinatura via Stripe CLI**

```bash
stripe subscriptions cancel <subscription_id>
```

Verificar no banco que `plano_id` voltou para `'free'`.

- [ ] **Step 6: Commit final**

```bash
git add .
git commit -m "feat: fluxo completo de billing com Stripe Checkout validado em modo teste"
```
